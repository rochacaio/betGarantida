PORT=3001
NODE_ENV=development
APP_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://user:password@localhost:5432/betgarantida
DIRECT_DATABASE_URL=postgresql://user:password@localhost:5432/betgarantida
SESSION_SECRET=replace-with-at-least-32-random-characters
CPF_HASH_SECRET=replace-with-a-separate-random-secret
# 32 bytes encoded as base64. Generate with: openssl rand -base64 32
CPF_ENCRYPTION_KEY=replace-with-a-32-byte-base64-key
SESSION_TTL_DAYS=30
PASSWORD_RESET_TTL_MINUTES=30
RESEND_API_KEY=
EMAIL_FROM=BetGarantida <nao-responda@example.com>
